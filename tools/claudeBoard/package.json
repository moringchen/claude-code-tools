{
  "name": "claude-board",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:ensure-daemon": "sh -c 'curl --noproxy \"*\" --silent --fail http://127.0.0.1:46123/snapshot > /dev/null || cargo run --manifest-path src-tauri/Cargo.toml --bin claude_boardd >/tmp/claude_boardd.log 2>&1 & until curl --noproxy \"*\" --silent --fail http://127.0.0.1:46123/snapshot > /dev/null; do sleep 1; done'",
    "build": "tsc && vite build",
    "test": "vitest run",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@testing-library/react": "^16.0.1",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "jsdom": "^25.0.1",
    "typescript": "^5.5.4",
    "vite": "^5.4.1",
    "vitest": "^2.0.5"
  }
}
